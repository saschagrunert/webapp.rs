//! All available routes within this application for fragment based routing

use yew_router::routes;

routes! {
    Error => "/error",
    Loading => "/loading",
    Login =>  "/login",
    Content => "/content",
}
