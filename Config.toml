[server]
ip = "127.0.0.1"
port = "30080"
tls = false
cert = "tls/cert.pem"
key = "tls/key.pem"

[log]
actix-web = "debug"
webapp = "trace"

[postgres]
host = "127.0.0.1"
username = "username"
password = ""
database = "database"

[api]
login-credentials = "/login/credentials"
login-session = "/login/session"
logout = "/logout"
